## Summary

为 `ray-use` 从零搭建内部 React UI 组件库 monorepo。业务项目可通过 workspace 引用 `@ray/ui`；设计/开发可在 Storybook 中预览 Button、Input、Card、Badge 及 light/dark 主题。

## Changes

### FEAT-f4db-react-ui-component: React UI 组件库骨架

**Added**
- 用户现在可以通过 `@ray/ui` 导入 Button、Input、Card、Badge 四个基础组件
- 用户现在可以在 Storybook 中浏览各组件 variant，并切换 light/dark 主题
- 产品活文档（design-system 模块）与 OpenSpec 类型合约

**Internal**
- pnpm monorepo（`packages/ui` + `apps/storybook`）
- shadcn/ui（Radix）+ Tailwind v4 + tsup 构建
- vitest 验收测试套件（8 tests）

## Review Dashboard

### 本次审计

| 维度 | 得分 | 状态 |
|------|------|------|
| 需求一致性 | 24/25 | ✅ |
| 安全性 | 19/20 | ✅ |
| 测试覆盖 | 18/20 | ✅ |
| 代码质量 | 17/20 | ✅ |
| 文档同步 | 14/15 | ✅ |
| **总分** | **92/100** | **通过** |

### 未解决技术债

- [ ] `packages/tokens` 独立包（v1 刻意不做）
- [ ] Button loading 态

## Acceptance Criteria

- [x] 根目录 `pnpm install` 成功
- [x] `pnpm --filter @ray/ui build` 成功
- [x] `pnpm --filter storybook dev` 可启动（`storybook build` 已验证）
- [x] `packages/ui/src/index.ts` 导出 Button、Input、Card、Badge
- [x] Button stories 含 default/secondary/destructive
- [x] Input、Card、Badge 各有基础 story
- [x] Storybook 支持 light/dark 主题
- [x] React 19 + TypeScript + Tailwind v4

## Test Evidence

- 测试结果：全部通过（8 tests）
- 审计得分：92/100
- Coder 迭代次数：1

---
🤖 Generated by [Ray](https://git.fulltrust.link/fe/ai-plugin-ray) pipeline
