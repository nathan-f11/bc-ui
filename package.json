{
  "name": "ray-use",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter storybook dev",
    "test": "pnpm --filter @ray/ui test && pnpm --filter storybook test",
    "test:e2e": "pnpm --filter storybook build && pnpm --filter e2e test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
