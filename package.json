{
  "name": "ray-use",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter storybook dev",
    "test": "pnpm --filter @ray/ui test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
